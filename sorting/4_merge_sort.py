
def merge(arr,start,mid,end):
	
	


def merge_sort(arr,star,end)
	mid = (start - mid)/2

	if(start < end):
		mid = (start - mid)/2
		merge_sort(arr,start,mid)
		merge_sort(arr,mid + 1,end)
		merge(arr,start,mid,end)

arr = []
merge_sort(arr)
print(arr)
